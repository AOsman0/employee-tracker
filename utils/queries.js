const allDepartmentsQuery = "SELECT department_name as Name FROM departments;";

const allRoles = "SELECT * FROM roles";

const allEmployees = "SELECT * FROM employees";

module.exports = { allDepartmentsQuery, allRoles, allEmployees };
